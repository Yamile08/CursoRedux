function data(state, action) {  //creamos la funcion que recibe dos parametros state y action
  switch (action.type) {
    case 'SEARCH_VIDEO': {
      return state
    }
    default:
      return state
  }
}

export default data;